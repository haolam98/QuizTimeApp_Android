<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiztimeapp">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/quiz_time_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/quiz_time_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".featue_onlineAccount.OnlineAccountActivity"></activity>
        <activity android:name=".feature_takequiz.AnswerQuestionsActivity" />
        <activity android:name=".feature_takequiz.TakeQuizActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".feature_set_and_share_quiz.ShareQuizActivity" />
        <activity android:name=".feature_makequiz.SaveAndChoiceActivity" />
        <activity android:name=".feature_makequiz.PreviewQuizActivity" />
        <activity android:name=".feature_makequiz.AddQuestionActivity" />
        <activity android:name=".feature_makequiz.MakeQuizActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPageActivity" />
    </application>

</manifest>